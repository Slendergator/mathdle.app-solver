# mathdle.app-solver
Python solver for Mathdle puzzles. Enter a target number and a set of base numbers, and it finds a sequence of +, -, *, / operations using all base numbers exactly once to reach the target.
